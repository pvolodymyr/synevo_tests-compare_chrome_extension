import SortOptions from "./SortOptions/SortOptions";

import * as SC from "./ToolBar.styled";

const ToolBar = () => {
  return (
    <SC.ToolBar>
      <SortOptions />
    </SC.ToolBar>
  );
};

export default ToolBar;
